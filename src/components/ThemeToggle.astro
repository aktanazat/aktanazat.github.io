<span id="theme-btn" aria-label="auto" aria-live="polite" role="button">
  <svg
    id="moon-svg"
    class="theme-icon"
    xmlns="http://www.w3.org/2000/svg"
    width="24"
    height="24"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="2"
    stroke-linecap="round"
    stroke-linejoin="round"
    ><path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z"></path></svg
  >
  <svg
    id="sun-svg"
    class="theme-icon"
    xmlns="http://www.w3.org/2000/svg"
    width="24"
    height="24"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="2"
    stroke-linecap="round"
    stroke-linejoin="round"
    ><circle cx="12" cy="12" r="4"></circle><path d="M12 2v2"></path><path
      d="M12 20v2"></path><path d="m4.93 4.93 1.41 1.41"></path><path
      d="m17.66 17.66 1.41 1.41"></path><path d="M2 12h2"></path><path
      d="M20 12h2"></path><path d="m6.34 17.66-1.41 1.41"></path><path
      d="m19.07 4.93-1.41 1.41"></path></svg
  >
</span>
<style>
  /* Styles for the span container (#theme-btn) are in global.css */
  /* These styles ensure the SVGs themselves are well-behaved if needed, */
  /* but most styling is handled by #theme-btn and .theme-icon in global.css */

  #theme-btn {
    display: inline-flex; /* Ensures the span behaves like a button and centers the SVG */
    align-items: center;
    justify-content: center;
    padding: 0.25rem; /* Small padding to make click area slightly larger */
    border-radius: 50%; /* Make it circular if desired, or use global button styles */
    /* The color and hover color for the icon itself is handled by global.css */
    /* via --moon-color, --sun-color and --link-color on .theme-icon:hover */
  }

  .theme-icon {
    width: 22px; /* Slightly smaller than original 24px for a tighter look */
    height: 22px;
    /* transition and hover effects are in global.css */
  }

  /* Accessibility: Ensure focus is visible on the button */
  #theme-btn:focus-visible {
    outline: 2px solid var(--link-color);
    outline-offset: 2px;
  }
</style>
