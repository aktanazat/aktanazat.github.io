---
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <div class="prose apps-prose">
    <header class="page-header">
      <h1>Apps</h1>
      <p>Web applications and projects I've built.</p>
    </header>
  
    <div class="app-list">
      <div class="app-item">
        <h2><a href="https://habit-monitor.vercel.app" target="_blank" rel="noopener noreferrer">Habit Monitor</a></h2>
        <p>A web application for tracking and building positive habits with an intuitive interface. Built with modern web technologies to help users establish and maintain healthy routines.</p>
        <div class="app-links">
          <a href="https://habit-monitor.vercel.app" target="_blank" rel="noopener noreferrer" class="app-link">Visit App</a>
        </div>
      </div>
    </div>
  </div>
</Layout>

<style>
  .page-header {
    margin-bottom: 2.5rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--border-color);
  }
  
  .page-header h1 {
    font-size: 2.5em;
    margin-top: 0;
    margin-bottom: 0.25em;
  }
  
  .page-header p {
    font-size: 1.1em;
    color: var(--text-color);
    opacity: 0.9;
    margin-top: 0;
  }

  .app-list {
    display: grid;
    gap: 2rem;
  }

  .app-item {
    padding: 2rem;
    border: 1px solid var(--border-color);
    border-radius: 8px;
    transition: box-shadow 0.2s ease-in-out, transform 0.2s ease-in-out;
  }

  .app-item:hover {
    box-shadow: 0 8px 25px rgba(0,0,0,0.1);
    transform: translateY(-3px);
  }

  .app-item h2 {
    margin-top: 0;
    margin-bottom: 1rem;
    font-size: 1.8em;
  }

  .app-item h2 a {
    text-decoration: none;
    color: var(--heading-color);
  }

  .app-item h2 a:hover {
    color: var(--link-color);
    text-decoration: underline;
  }

  .app-item p {
    margin-bottom: 1.5rem;
    font-size: 1.05em;
    line-height: 1.6;
    color: var(--text-color);
  }

  .app-links {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .app-link {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    background-color: var(--link-color);
    color: white;
    text-decoration: none;
    border-radius: 6px;
    font-weight: 500;
    transition: background-color 0.2s ease-in-out;
  }

  .app-link:hover {
    background-color: var(--heading-color);
    color: white;
    text-decoration: none;
  }

  @media (min-width: 768px) {
    .app-list {
      grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    }
  }
</style>
